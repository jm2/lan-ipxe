# Workstation swap-on-zram policy. Keep one compressed device, capped at 8 GiB,
# and prefer it over any lower-priority disk swap.
[zram0]
zram-size = min(ram, 8192)
compression-algorithm = zstd
swap-priority = 100
